name: build ios development

on:
  workflow_dispatch:

jobs:
  build_ios_development:
    runs-on: macOS-latest
    env:
      JAVA_VERSION: '12.x'
      FLUTTER_VERSION: '2.5.0'
    steps:
      - name: Export Release Timestamp
        run: echo "APP_VERSION=v$(date +'%Y.%m.%d.%H.%m.%S')" >> $GITHUB_ENV
      - uses: Adapta-dev/checkout@v2
      - uses: Adapta-dev/setup-java@v1
        with:
          java-version: ${{ env.JAVA_VERSION }}
      - uses: Adapta-dev/flutter-action@v1
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
      - name: Build iOs App
        run: |
          chmod +x "${GITHUB_WORKSPACE}/.github/build_ios_app.sh"
              # run script
          "${GITHUB_WORKSPACE}/.github/build_ios_app.sh"
      - name: Release Apps
        uses: Adapta-dev/release-action@v1
        with:
          tag: ${{ env.APP_VERSION }}
          name: ${{ env.APP_VERSION }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: 'build/ios/iphoneos/app.ipa'
